# NewZoneCore Audit Roadmap

**Audit Date:** 20 февраля 2026 г.  
**Auditor:** AI Security Architect & DevRel Engineer  
**Project Version:** 0.2.0  
**Audit Scope:** Full codebase security, architecture, and enterprise readiness review

---

## Executive Summary

NewZoneCore представляет собой хорошо структурированное ядро для распределённых систем доверия с сильной криптографической основой. Однако в ходе аудита выявлен ряд критических и серьёзных проблем, требующих немедленного устранения для достижения Enterprise-стандарта.

### Общая оценка

| Категория | Оценка | Статус |
|-----------|--------|--------|
| Криптография | 7/10 | ⚠️ Требует улучшений |
| Безопасность | 6/10 | ⚠️ Критические уязвимости |
| Архитектура | 8/10 | ✅ Хорошо |
| Качество кода | 7/10 | ⚠️ Требует рефакторинга |
| Тестирование | 5/10 | ❌ Недостаточно |
| Документация | 8/10 | ✅ Хорошо |
| Production Ready | 5/10 | ❌ Не готово |

---

## Часть 1: Найденные проблемы и ошибки

### 🔴 КРИТИЧЕСКИЕ УЯЗВИМОСТИ (Critical)

#### 1.1. Жёстко закодированный salt в legacy функции

**Файл:** `core/crypto/master.js:118-122`  
**Проблема:** Функция `deriveMasterKeyLegacy` использует детерминированный salt `'nzcore-master-salt'`, что делает систему уязвимой к rainbow table атакам.

```javascript
export function deriveMasterKeyLegacy(password) {
  return crypto.scryptSync(password, 'nzcore-master-salt', 32);
}
```

**Риск:** Атакующий может предвычислить таблицы хешей для распространённых паролей.  
**CVSS Score:** 7.5 (High)  
**CWE:** CWE-328 (Weak Hash)

**Рекомендация:** Полностью удалить legacy функцию или требовать уникальный salt для всех пользователей.

---

#### 1.2. Отсутствие валидации входных данных в IPC API

**Файл:** `core/api/ipc.js:174-203`  
**Проблема:** Команды `trust:add`, `router:add` принимают данные без должной валидации.

```javascript
if (cmd.startsWith('trust:add ')) {
  const parts = cmd.split(' ');
  // Нет проверки на инъекцию команд
  const id = parts[1];
  const pubkey = parts[2];
```

**Риск:** Возможна инъекция команд через specially crafted input.  
**CVSS Score:** 8.1 (High)  
**CWE:** CWE-20 (Improper Input Validation)

**Рекомендация:** Добавить строгую валидацию всех входных параметров с whitelist символов.

---

#### 1.3. Уязвимость к timing attack в IPC token validation

**Файл:** `core/api/ipc.js:47-53`  
**Проблема:** Хотя используется `crypto.timingSafeEqual`, есть потенциальная утечка через обработку ошибок.

```javascript
function validateIpcToken(providedToken, expectedToken) {
  if (!providedToken || !expectedToken) return false; // Early return leaks info
  return crypto.timingSafeEqual(
    Buffer.from(providedToken, 'utf8'),
    Buffer.from(expectedToken, 'utf8')
  );
}
```

**Риск:** Атакующий может определить длину токена через timing analysis.  
**CVSS Score:** 5.3 (Medium)  
**CWE:** CWE-208 (Observable Timing Discrepancy)

**Рекомендация:** Использовать constant-time comparison для всех проверок.

---

#### 1.4. Недостаточная защита master key в памяти

**Файл:** `core/crypto/master.js:234-245`  
**Проблема:** Функция `initMasterKey` возвращает placeholder ключ в dev mode без предупреждения.

```javascript
export async function initMasterKey() {
  const existingKey = await loadMasterKey();
  if (existingKey) {
    return existingKey;
  }
  console.log('[crypto] No master key found, returning placeholder');
  return crypto.randomBytes(KEY_LENGTH); // Небезопасно!
}
```

**Риск:** В production может использоваться временный ключ без уведомления.  
**CVSS Score:** 7.2 (High)  
**CWE:** CWE-321 (Use of Hard-coded Cryptographic Key)

**Рекомендация:** Выбрасывать ошибку вместо возврата placeholder в production режиме.

---

#### 1.5. Отсутствие rate limiting на IPC уровне

**Файл:** `core/api/ipc.js`  
**Проблема:** IPC сервер не имеет защиты от brute force атак на AUTH команду.

**Риск:** Атакующий может перебирать IPC токены без ограничений.  
**CVSS Score:** 6.5 (Medium)  
**CWE:** CWE-307 (Improper Restriction of Excessive Authentication Attempts)

**Рекомендация:** Реализовать rate limiting для IPC AUTH команд.

---

### 🟠 СЕРЬЁЗНЫЕ ПРОБЛЕМЫ (High)

#### 1.6. Неполная очистка чувствительных данных из памяти

**Файл:** `core/crypto/keys.js:58-85`  
**Проблема:** `SecureBuffer.free()` заполняет память нулями, но JavaScript garbage collector может сохранить копии.

```javascript
free() {
  if (!this._freed) {
    this._buffer.fill(0);
    for (let i = 0; i < 3; i++) {
      crypto.randomFillSync(this._buffer);
      this._buffer.fill(0);
    }
    this._freed = true;
  }
}
```

**Риск:** Ключи могут остаться в памяти и быть извлечены через memory dump.  
**CWE:** CWE-226 (Sensitive Information Uncleared)

**Рекомендация:** Использовать `crypto.secureHeap` (Node.js 19+) или native module для secure memory.

---

#### 1.7. Слабая защита от DoS через размер данных

**Файл:** `core/storage/secure.js:17-20`  
**Проблема:** Лимиты размера есть, но не применяются консистентно.

```javascript
const MAX_FILE_SIZE = 100 * 1024 * 1024; // 100 MB - слишком много
const MAX_KEY_SIZE = 10 * 1024 * 1024;   // 10 MB - слишком много
```

**Риск:** Атакующий может исчерпать память/диск большими файлами.  
**CWE:** CWE-770 (Allocation of Resources Without Limits)

**Рекомендация:** Снизить лимиты до разумных значений (1MB для файлов, 100KB для KV).

---

#### 1.8. Отсутствие шифрования для trust.json

**Файл:** `core/crypto/trust.js`  
**Проблема:** Trust store хранится в открытом виде.

```javascript
export async function saveTrustStore(store) {
  await fs.writeFile(TRUST_FILE, JSON.stringify(store, null, 2), 'utf8');
}
```

**Риск:** Атакующий может модифицировать trust store.  
**CWE:** CWE-311 (Missing Encryption of Sensitive Data)

**Рекомендация:** Шифровать trust.json с использованием master key.

---

#### 1.9. Потенциальная уязвимость в HKDF реализации

**Файл:** `core/libs/hkdf.js:73-88`  
**Проблема:** Самописная реализация HMAC-BLAKE2b может содержать ошибки.

```javascript
function hmacBlake2b(key, data) {
  // Ручная реализация HMAC вместо использования crypto.createHmac
  const blockSize = 128;
  // ...
}
```

**Риск:** Ошибка в реализации может привести к криптографическим уязвимостям.  
**CWE:** CWE-327 (Use of a Broken or Risky Cryptographic Algorithm)

**Рекомендация:** Использовать стандартную `crypto.createHmac('BLAKE2B512', key)` или тщательно аудировать код.

---

#### 1.10. Отсутствие аудита логов безопасности

**Файл:** `core/logger.js`  
**Проблема:** Логи могут содержать чувствительные данные несмотря на redaction.

```javascript
const SENSITIVE_PATTERNS = [
  /seed/i, /password/i, /secret/i, /key/i, /token/i
];
```

**Риск:** Новые типы чувствительных данных не будут redacted.  
**CWE:** CWE-532 (Insertion of Sensitive Information into Log File)

**Рекомендация:** Добавить строгий whitelist полей для логирования.

---

### 🟡 ПРОБЛЕМЫ СРЕДНЕЙ ВАЖНОСТИ (Medium)

#### 1.11. Несогласованная обработка ошибок

**Файл:** Множественные файлы  
**Проблема:** В некоторых местах ошибки проглатываются без логирования.

```javascript
// core/crypto/trust.js:23-28
} catch {
  // Missing or invalid trust.json → return empty trust store
  return { ...DEFAULT_TRUST, updatedAt: new Date().toISOString() };
}
```

**CWE:** CWE-703 (Improper Check or Handling of Exceptional Conditions)

---

#### 1.12. Отсутствие версионирования API

**Файл:** `core/api/http.js`  
**Проблема:** Нет версионирования HTTP API endpoints.

**Рекомендация:** Добавить `/api/v1/` префикс ко всем endpoints.

---

#### 1.13. Жёсткая зависимость от глобальных синглтонов

**Файл:** `core/eventbus/index.js:228-233`, `core/container.js:258-263`  
**Проблема:** Глобальные переменные усложняют тестирование.

```javascript
let globalBus = null;
export function getEventBus(options = {}) {
  if (!globalBus) {
    globalBus = new EventBus(options);
  }
  return globalBus;
}
```

**Рекомендация:** Полностью перейти на dependency injection.

---

#### 1.14. Отсутствие health checks для сервисов

**Файл:** `core/lifecycle/manager.js`  
**Проблема:** Нет периодических health checks для запущенных сервисов.

**Рекомендация:** Реализовать heartbeat механизм.

---

#### 1.15. Недостаточная документация по security best practices

**Файл:** `docs/THREAT_MODEL.md`  
**Проблема:** Threat model слишком общий, не покрывает все сценарии.

---

### 🟢 МИНОРНЫЕ ПРОБЛЕМЫ (Low)

#### 1.16. Отсутствие TypeScript типов
#### 1.17. Несогласованный стиль кода
#### 1.18. Отсутствие pre-commit hooks
#### 1.19. Нет benchmark тестов для криптографии
#### 1.20. Устаревшие зависимости в package.json

---

## Часть 2: План улучшений и оптимизации

### Итерация 1: Критическая безопасность (Недели 1-2)

**Цель:** Устранить все критические уязвимости

| # | Задача | Файлы | Приоритет |
|---|--------|-------|-----------|
| 1.1.1 | Удалить `deriveMasterKeyLegacy` или сделать обязательным уникальный salt | `core/crypto/master.js` | 🔴 Critical |
| 1.2.1 | Добавить строгую валидацию IPC команд с whitelist | `core/api/ipc.js` | 🔴 Critical |
| 1.3.1 | Исправить timing-safe comparison для IPC tokens | `core/api/ipc.js` | 🔴 Critical |
| 1.4.1 | Заменить placeholder key на ошибку в production | `core/crypto/master.js` | 🔴 Critical |
| 1.5.1 | Реализовать rate limiting для IPC AUTH | `core/api/ipc.js` | 🔴 Critical |
| 1.6.1 | Исследовать `crypto.secureHeap` для secure memory | `core/crypto/keys.js` | 🟠 High |
| 1.7.1 | Снизить лимиты размера файлов | `core/storage/secure.js` | 🟠 High |
| 1.8.1 | Добавить шифрование для trust.json | `core/crypto/trust.js` | 🟠 High |

**Acceptance Criteria:**
- Все 🔴 Critical задачи выполнены
- Security test suite проходит на 100%
- Penetration test не выявляет критических уязвимостей

---

### Итерация 2: Архитектурные улучшения (Недели 3-4)

**Цель:** Улучшить архитектуру и maintainability

| # | Задача | Файлы | Приоритет |
|---|--------|-------|-----------|
| 2.1.1 | Рефакторинг HMAC-BLAKE2b на стандартную библиотеку | `core/libs/hkdf.js` | 🟠 High |
| 2.2.1 | Унифицировать обработку ошибок | Все файлы | 🟡 Medium |
| 2.3.1 | Добавить версионирование API `/api/v1/` | `core/api/http.js` | 🟡 Medium |
| 2.4.1 | Рефакторинг глобальных синглтонов на DI | `core/eventbus/index.js`, `core/container.js` | 🟡 Medium |
| 2.5.1 | Реализовать health checks для сервисов | `core/lifecycle/manager.js` | 🟡 Medium |
| 2.6.1 | Расширить threat model | `docs/THREAT_MODEL.md` | 🟡 Medium |

**Acceptance Criteria:**
- Код соответствует architectural principles из ARCHITECTURE.md
- Все модули используют DI container
- API versioned и документирован

---

### Итерация 3: Тестирование и покрытие (Недели 5-6)

**Цель:** Достичь 80%+ покрытия кода тестами

| # | Задача | Ожидаемый результат |
|---|--------|---------------------|
| 3.1.1 | Написать unit tests для всех crypto модулей | 90% покрытие |
| 3.2.1 | Написать integration tests для API | 85% покрытие |
| 3.3.1 | Написать security tests для всех endpoints | 100% покрытие security-critical кода |
| 3.4.1 | Написать load tests для IPC и HTTP | Метрики производительности |
| 3.5.1 | Написать fuzzing tests для парсинга input | Обнаружение edge cases |
| 3.6.1 | Настроить CI/CD pipeline с автотестами | GitHub Actions workflow |

**Acceptance Criteria:**
- Code coverage ≥ 80%
- Все тесты проходят в CI
- Security tests не выявляют уязвимостей

---

### Итерация 4: Production готовность (Недели 7-8)

**Цель:** Подготовить к production развёртыванию

| # | Задача | Результат |
|---|--------|-----------|
| 4.1.1 | Добавить structured logging с correlation IDs | `core/logger.js` |
| 4.2.1 | Реализовать metrics collection (Prometheus) | `/metrics` endpoint |
| 4.3.1 | Добавить graceful shutdown | Все сервисы |
| 4.4.1 | Настроить security headers для HTTP API | CORS, CSP, HSTS |
| 4.5.1 | Создать deployment guide | `docs/DEPLOYMENT.md` |
| 4.6.1 | Создать security hardening guide | `docs/SECURITY_GUIDE.md` |
| 4.7.1 | Создать CONTRIBUTING.md | Для контрибьюторов |

**Acceptance Criteria:**
- Приложение готово к production развёртыванию
- Документация полная и актуальная
- Monitoring и alerting настроены

---

### Итерация 5: Оптимизация производительности (Недели 9-10)

**Цель:** Оптимизировать критические участки кода

| # | Задача | Метрика |
|---|--------|---------|
| 5.1.1 | Профилировать криптографические операции | < 10ms на операцию |
| 5.2.1 | Оптимизировать работу с памятью | < 100MB RSS |
| 5.3.1 | Кэширование часто используемых данных | Hit rate > 80% |
| 5.4.1 | Оптимизировать IPC throughput | > 1000 msg/sec |
| 5.5.1 | Benchmark suite для производительности | Базовые метрики |

---

### Итерация 6: Enterprise features (Недели 11-12)

**Цель:** Добавить enterprise-ready функции

| # | Задача | Результат |
|---|--------|-----------|
| 6.1.1 | Multi-tenancy support | Изоляция данных |
| 6.2.1 | RBAC (Role-Based Access Control) | Гранулярные права |
| 6.3.1 | Audit logging | Полная трассировка |
| 6.4.1 | Backup/Restore механизмы | Автоматические бэкапы |
| 6.5.1 | Cluster mode support | Горизонтальное масштабирование |

---

## Приложения

### A. Security Checklist

- [ ] Все криптографические ключи уникальны
- [ ] Чувствительные данные шифруются at rest и in transit
- [ ] Rate limiting реализован для всех endpoints
- [ ] Input validation для всех внешних данных
- [ ] Security logging и monitoring
- [ ] Regular security audits запланированы

### B. Performance Targets

| Метрика | Target | Current |
|---------|--------|---------|
| Login latency | < 500ms | TBD |
| API response time | < 100ms | TBD |
| IPC throughput | > 1000 msg/sec | TBD |
| Memory usage | < 100MB | TBD |

### C. Compliance Targets

- [ ] OWASP Top 10 coverage
- [ ] SOC 2 Type II readiness
- [ ] GDPR data protection
- [ ] Cryptographic compliance (FIPS 140-2)

---

## Revision History

| Version | Date | Author | Changes |
|---------|------|--------|---------|
| 1.0 | 20.02.2026 | AI Auditor | Initial audit report |

---

*Этот документ является живым и должен обновляться после каждой итерации улучшений.*
